/** Re-export shared surface types */
export type { AppSurface } from './hostname';

/** Experience app role (different from admin RBAC) */
export type ExperienceRole = 'member' | 'trainer' | 'staff';
